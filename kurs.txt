veri türleri
-string=harf kelime cümşe
-integer=tam sayı 3,5,7
-double veya float =3.14 , 5.99
-boolean=treu veya false

var=global olarak her yerde kullanılır
let= fonksiyonların içinde kullanılır.
const=sabit değişkenleri tutar.

operatörler
=atama
==sorgulama
===türüne göre sorgulama
% mod alma

mod kalanı gösterir.
Dizi ve fonksionlar=heap da tutulur.

var number =prompt("sayı giriniz")
number=parseInt(number)
if(number >1 && number <=100){
 if(number < 1 && number <=50){
 if(

